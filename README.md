# Recursion algorithm checkpoint

For This checkpoint i created an algorithm that uses a loop to compare characters from the beginning and end of the word and returns if the word is palindrome or not.
